rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder rules for Sprint 1 scaffolding
    // NOTE: Replace with proper security rules before production.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
